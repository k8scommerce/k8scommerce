info(
	title: "Customer API Endpoints"
	desc: "admin gateway customer api endpoints"
	author: "localrivet"
	email: "localrivet"
	version: "1"
)

// objects
type Customer {
	Id        int64  `json:"id"`
	FirstName string `json:"firstName"`
	LastName  string `json:"lastName"`
	Email     string `json:"email"`
	Password  string `json:"password,omitempty"`
}

//
// requests & responses
//

//
// endpoints
//

@server(
	middleware: Locale
)
service admin {

}